/**
 * Allows sharing of UI components between routes
 * Reuse pages that are only in posts components
 */

import React from 'react'

const layout = () => {
  return (
    <div>Navigate to Top</div>
  )
}

export default layout